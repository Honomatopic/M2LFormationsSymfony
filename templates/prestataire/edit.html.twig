{% extends 'base.html.twig' %}

{% block body %}
    <h1>Pour éditer le prestataire</h1>
    {% for message in app.flashes('echoue') %}
    <div class="echoue">
      {{ message }}
    </div>
  {% endfor %}
    {{ include('prestataire/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('prestataire_index') }}">&#8617;&#65039;Retour à la liste des prestataires</a>

    {{ include('prestataire/_delete_form.html.twig') }}
{% endblock %}
