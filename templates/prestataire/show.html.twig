{% extends 'base.html.twig' %}

{% block body %}
    <h1>Le Prestataire {{ prestataire.nom }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prestataire.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ prestataire.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('prestataire_index') }}">&#8617;&#65039;Retour à la liste des prestataires</a>

    <a href="{{ path('prestataire_edit', {'id': prestataire.id}) }}">&#128395;&#65039;Modifier</a>

    {{ include('prestataire/_delete_form.html.twig') }}
{% endblock %}
