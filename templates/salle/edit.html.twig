{% extends 'base.html.twig' %}

{% block body %}
    <h1>Pour éditer la salle</h1>
    {% for message in app.flashes('echoue') %}
    <div class="echoue">
      {{ message }}
    </div>
  {% endfor %}
    {{ include('salle/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('salle_index') }}">&#8617;&#65039;Retour à la liste des salles</a>

    {{ include('salle/_delete_form.html.twig') }}
{% endblock %}
