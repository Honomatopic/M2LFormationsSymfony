{% extends 'base.html.twig' %}

{% block body %}
    <h1>La {{ salle.nom }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ salle.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ salle.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('salle_index') }}">&#8617;&#65039;Retour à la liste des salles</a>

    <a href="{{ path('salle_edit', {'id': salle.id}) }}">&#128395;&#65039;Modifier</a>

    {{ include('salle/_delete_form.html.twig') }}
{% endblock %}
