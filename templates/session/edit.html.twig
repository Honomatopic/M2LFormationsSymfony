{% extends 'base.html.twig' %}

{% block body %}
    <h1>Pour éditer la session de formation</h1>
    {% for message in app.flashes('echoue') %}
    <div class="echoue">
      {{ message }}
    </div>
  {% endfor %}
    {{ include('session/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('session_index') }}">&#8617;&#65039;Retour à la liste des sessions</a>

    {{ include('session/_delete_form.html.twig') }}
{% endblock %}
