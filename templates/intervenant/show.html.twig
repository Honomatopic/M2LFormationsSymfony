{% extends 'base.html.twig' %} {% block body %}
<h1>L'intervenant {{ intervenant.nom }}</h1>

<table class="table">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ intervenant.id }}</td>
        </tr>
        <tr>
            <th>Prénom et Nom</th>
            <td>{{ intervenant.nom }}</td>
        </tr>

    </tbody>
</table>

<a href="{{ path('intervenant_index') }}">&#8617;&#65039;Retour à la liste des intervenants</a>

<a href="{{ path('intervenant_edit', {'id': intervenant.id}) }}">&#128395;&#65039;Modifier</a> {{ include('intervenant/_delete_form.html.twig') }} {% endblock %}