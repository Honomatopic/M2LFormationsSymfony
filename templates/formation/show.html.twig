{% extends 'base.html.twig' %}

{% block body %}

  <h2>La 
    {{ formation.intitule }}</h2>

  <table class="table">
    <tbody>
      <tr>
        <th>Id</th>
        <td>{{ formation.id }}</td>
      </tr>
      <tr>
        <th>Intitule</th>
        <td>{{ formation.intitule }}</td>
      </tr>
    </tbody>
  </table>
  <a href="{{ path('formation_index') }}">&#8617;&#65039;Retour à la liste des formations</a>
  {% if is_granted('ROLE_ADMIN') %}
    <a href="{{ path('formation_edit', {'id': formation.id}) }}">&#128395;&#65039;Modifier</a>
  {% endif %}

  {{ include('formation/_delete_form.html.twig') }}
{% endblock %}
